var Menu = require('./Menu');
var React = require('react');

var Demo = React.createClass({
  render: function() {
    return (
      <div>
        <Menu />
      </div>
    );
  }
});

module.exports = Demo;
